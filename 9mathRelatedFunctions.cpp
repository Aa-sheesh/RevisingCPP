#include <iostream>
#include <cmath>

int main(){
    double x=4.7;
    double y=1.5;
    double z;

    // z=std::min(x,y);
    // z=std::max(x,y);
    // z=pow(2,4);
    // z=sqrt(16);
    // z=abs(-3);
    // z=round(x);
    // z=ceil(x);
    // z=floor(x);

    // std::cout<<z;

    return 0;
}
